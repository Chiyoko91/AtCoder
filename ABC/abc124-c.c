#include<stdio.h>
#include<string.h>
#define DIM 100000


int main(void)
{
char S[DIM]={};

int one,zero,i,len;

scanf("%s",S);

len = strlen(S);

for(i=0;i<len;i++)
{
  if(S[i]%2==0)
  {
    
  }
  else
  {

  }
}

  return 0;
}
